---
layout: base.njk
title: "Best Pet Products and Smart Pet Tech"
description: "GPS trackers, automatic feeders, self-cleaning litter boxes, and the best smart tech gear for your pets. Honest reviews to keep your animals happy and your life easier."
permalink: /pet-tech/index.html
categorySlug: pet-tech
eleventyExcludeFromCollections: true
---

<section class="hub-hero">
  <div class="section-inner">
    <nav class="hub-breadcrumb" aria-label="Breadcrumb">
      <a href="{{ '/' | url }}">Home</a>
      <span aria-hidden="true"> › </span>
      <span>Pet Tech</span>
    </nav>
    <h1>Best Pet Products and Smart Pet Tech</h1>
    <p>GPS pet trackers, automatic feeders, self-cleaning litter boxes, and smart pet accessories reviewed honestly. Find the gear that actually makes life with pets easier.</p>
  </div>
</section>

<section class="section">
  <div class="section-inner">
    <div class="article-grid">
      {% for article in collections.articlesByDate %}
        {% if article.data.category == "pet-tech" %}
        <article class="article-card">
          <div class="card-body">
            <span class="card-category">Pet Tech</span>
            <a href="{{ article.url | url }}" style="display:block">
              <h2 class="card-title">{{ article.data.title }}</h2>
            </a>
            <p class="card-excerpt">{{ article.data.description | truncate(120) }}</p>
            <div class="card-meta">
              <span>{{ article.date | postDate }}</span>
              {% if article.data.readTime %}<span>·</span><span>{{ article.data.readTime }} min read</span>{% endif %}
            </div>
            <a href="{{ article.url | url }}" class="read-more" style="margin-top:0.75rem;display:inline-flex">Read guide →</a>
          </div>
        </article>
        {% endif %}
      {% endfor %}
    </div>
  </div>
</section>
